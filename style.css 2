/* Reset */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

/* Body background gradient */
body {
  height: 100vh;
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  background: linear-gradient(to bottom, #bbaecc, #4f4564);
  display: flex;
  flex-direction: column;
}

/* HEADER */
header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  background: linear-gradient(to bottom, #d7caea, #5c4cac);
  border-bottom: 3px solid black;
  padding: 1rem 2rem;
}

/* Logo Section */
.logo-section {
  display: flex;
  align-items: center;
  gap: 0.7rem;
}

.logo-icon {
  border: 2px solid black;
  border-radius: 50%;
  width: 38px;
  height: 38px;
  font-weight: bold;
  font-size: 1.8rem;
  line-height: 38px;
  text-align: center;
  color: black;
  user-select: none;
  font-family: Arial, sans-serif;
}

.logo-text {
  font-size: 2rem;
  font-weight: 900;
  color: #220a81; /* dark purple */
  text-shadow: 1px 1px 0 #000;
  user-select: none;
  font-family: 'Arial Black', Arial, sans-serif;
  text-transform: uppercase;
}

/* Navigation */
nav {
  display: flex;
  align-items: center;
  gap: 2rem;
}

.home-link {
  color: #300a81;
  font-size: 1.1rem;
  font-weight: 600;
  text-decoration: underline;
  user-select: none;
}

.info-icon {
  border-radius: 50%;
  border: 2px solid black;
  width: 38px;
  height: 38px;
  font-weight: bold;
  font-size: 1.8rem;
  line-height: 38px;
  text-align: center;
  color: black;
  user-select: none;
  font-family: Arial, sans-serif;
}

/* MAIN CONTENT */
main {
  flex-grow: 1;
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 2rem;
}

/* LOGIN BOX */
.login-box {
  background: rgba(204, 187, 211, 0.85);
  border-radius: 30px;
  width: 330px;
  padding: 3rem 3rem 3.5rem 3rem;
  box-shadow: inset 0 0 25px rgb(115 96 138 / 55%);
  text-align: center;
  backdrop-filter: blur(4px);
}

/* LOGIN TITLE */
.login-box h1 {
  font-size: 2.7rem;
  margin-bottom: 2rem;
  color: #220a81;
  font-weight: 900;
  text-shadow: 1px 1px 0 black;
  user-select: none;
  letter-spacing: 1.5px;
}

/* INPUT FIELDS */
.login-box input[type="email"],
.login-box input[type="password"] {
  display: block;
  width: 100%;
  padding: 12px 15px;
  margin-bottom: 1.8rem;
  border-radius: 15px;
  border: none;
  outline: none;
  font-size: 1rem;
  color: #5a3f87;
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

.login-box input::placeholder {
  color: #7d6aa6;
}

/* LINKS container below inputs */
.links {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 0.8rem;
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  color: #5a3f87;
  font-size: 1rem;
  margin-bottom: 2.8rem;
}

.links a {
  color: #5a3f87;
  text-decoration: underline;
  cursor: pointer;
}

.links span {
  color: black;
  user-select: none;
}

/* SIGN IN BUTTON */
.sign-in-btn {
  background-color: rgba(123, 101, 175, 0.5);
  border: none;
  border-radius: 25px;
  padding: 10px 28px;
  font-size: 1.1rem;
  color: #3b2e87;
  cursor: pointer;
  font-weight: 600;
  transition: background-color 0.3s ease;
  user-select: none;
}

.sign-in-btn:hover {
  background-color: rgba(123, 101, 175, 0.8);
}
